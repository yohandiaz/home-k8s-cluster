apiVersion: v2
name: to-do
description: A Helm chart for a FastAPI to-do application
version: 0.0.3
appVersion: "0.0.2"

dependencies:
  - name: ingress-nginx
    version: "4.11.1"
    repository: "https://kubernetes.github.io/ingress-nginx"
